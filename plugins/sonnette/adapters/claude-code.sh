#!/bin/bash
#
# Claude Code adapter — stub.
#
# CC sessions use /open and /close skills for lifecycle.
# The conductor can start CC with an initial prompt via `claude -p`,
# but enforcement is behavioural (skills) not structural (extension).
#
# TODO: Implement when CC adapter is needed.
#

echo "Claude Code adapter not yet implemented." >&2
echo "Use Pi adapter or run CC manually with /open and /close skills." >&2
exit 1
