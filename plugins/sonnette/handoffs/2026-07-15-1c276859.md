# Handoff — 2026-07-15

session_id: 1c276859-eca0-43e3-a692-95552442f1c2
purpose: Pick the next Bun brick — became two shipped phases (bun canonical + delivery truth) and a roster-ghost root-cause + fix.
format: fond-v1

## For the next Claude

### Done
- **Investigated the node-upgrade fork → Option B (mesh is bun-only; do NOT upgrade node).** Root cause: tube's node is apt-capped at 20.19 (no built-in WebSocket) and system node is load-bearing for gueridon's native `node-pty` (prod, phone UI). Encoded on the board: `aby-giwohi` re-scoped with the full ws-drop audit, `aby-bosuwa` stale premise fixed, and the tube re-order (Phase 3 BEFORE Phase 2) recorded at both ends.
- **Phase 3 (aby-bosuwa) DONE + pushed (`5ee482b`).** `.mcp.json` + sonnette `.claude-plugin/plugin.json` both flipped `node dist/…js` → `bun src/conductor-channel.ts`; `.mcp.bun.json` deleted (redundant). bun reached via a portable `~/.bun/bin/bun → ~/.local/bin/bun` symlink (that dir is on the MCP-spawn PATH; `~/.bun/bin` is not). Verified live (fresh probe spawned bun, joined mesh, saw the node session) and **soaked 27 min clean** under real load.
- **aby-nowabu DONE + pushed (`693bc63`).** `ConductorBridge.sendAndConfirm()` races a ~200ms window against the server's `conductor_error` (no correlation id → attribution by construction, sends are serial); `send_message` now returns `delivered to X (server accepted)` / `NOT delivered to X: <error>` instead of minting `sent`. **Suite 58/58.** Verified at the bridge layer (live two-bridge test) AND end-to-end through the tool (probe to a ghost returned "Agent not found").
- **Root-caused the roster ghost** (see For Claudes to come) and reconciled the concurrent **sibling session `37b7619d`** that committed the mesh-id refactor (`d424083`/`eb6c357`) and /closed mid-my-session — nothing lost, my intended commit was correctly redundant.
- **Doc + board hygiene:** `CLAUDE.md` `.mcp.json` example → bun/src (+ bun-on-PATH note); `understanding.md` surgical fact-fixes (header date, `:87` wozuvi→nowabu-shipped, "Tube is home" → Phase 3 done / bun canonical); `aby-zufefu` --how updated (dist blocker resolved by Phase 3; **bun-on-PATH is the new fleet crux**); `aby-paluvu` --how gains the distinct-ghost cross-ref.

### Reflection
- The spine was **diagnose-to-root from the substrate, not theory**, twice: the fork (bon `--how` + gueridon's node-pty) and the ghost (the WIRE `events.jsonl` + the CODE). Every claim grounded in wire/proc/code.
- Sameer's "ok B **if you're sure**" earned a real confidence check — the killer risk (does the bridge use ws-specific ping/pong the built-in WebSocket can't do?) was **disproven** (keepalive is an app-level JSON ping over `.send()`) before committing to B.
- A grep-artifact near-miss: I searched `\.bun` and "found" the PATH line missing — it uses `$BUN_INSTALL`, so grep couldn't match it. Reading the real file before editing caught it. (The real issue was different: bun absent from the MCP-spawn PATH.)
- The working tree changing under me (the sibling) → explore-before-concluding paid off; I checked `lsof`/`/proc`/`mesh_peers` before touching shared surfaces.

### Uncertain
- **Ghost persistence unmeasured.** I asked the bun peer to re-run `mesh_peers` over time (does `a5e4252f` age out or persist?); it cross-checked processes instead. Hypothesis: permanent for that session's life (the expire event is in the past and never arrives). Low stakes — nowabu makes it harmless.

### Risks
- **bun-on-PATH is machine-state** (the `~/.local/bin/bun` symlink) — persists across reboot on tube but is only documented in understanding.md / aby-zufefu. A fleet machine without bun on a standard PATH dir = **silent** mesh death (MCP fails soft). aby-zufefu carries the requirement.
- **Both currently-running sessions predate the nowabu fix** (my node session + the bun soak, both loaded before the edit) — they still mint the old "sent" until restarted. Fresh sessions are covered (bun runs src, no build).

### Opportunities
- **aby-giwohi (drop ws → built-in WebSocket)** — the next Bun brick, now UNBLOCKED (bun canonical is its prerequisite). Full audit in its brief; do it ON a bun session. Sharp edge: the `close` handler's `(code, reason)` → `CloseEvent` mapping, which the aby-tarafo supersession fix depends on. `ws`'s keepalive is app-level JSON ping, unaffected. Keep `ws` revertible as the safety net (no live node cross-check needed — the de-risk is testable under bun alone).
- **aby-pizufo** (Phase 4: tests under `bun test`), **aby-fiwato** (Phase 5: hot-path no-rebuild), **aby-lezuhu** (Phase 6: docs) — remaining Bun phases.
- **aby-zufefu** (sonnette plugin — dist blocker RESOLVED by Phase 3; bun-on-PATH is the new crux), **aby-lesefu** (managed-settings allowlist), **aby-wodagu** (inbound survives resume).
- **aby-rinisa** (convergence design-review) — inputs now settled.

## For Claudes to come

- **bun is the canonical mesh runtime (Phase 3).** `.mcp.json` and the sonnette `plugin.json` run `bun src/conductor-channel.ts` directly — no build step, no `dist/`. bun is reached via a `~/.local/bin/bun` symlink because that dir is on the CC MCP-spawn PATH and `~/.bun/bin` is not. **Fleet rule:** every machine needs bun on a standard PATH dir or the channel MCP server ENOENTs *silently* (MCP fails soft — no error, just absent tools). The runtime decision was Option B: mesh bun-only; node NOT upgraded because gueridon (prod) rides system node's native `node-pty`.
- **send_message reports delivery truth (aby-nowabu).** The mesh has no store-and-forward: a send to an absent/ghost peer is rejected synchronously with `conductor_error: "Agent not found"` (~25ms). `ConductorBridge.sendAndConfirm()` races a ~200ms window against the bridge's `error` event and the tool returns delivered / NOT-delivered. `conductor_error` has NO correlation id — attribution is by construction (the tool sends serially). `{ok:true}` = server accepted, NOT an end-to-end receipt.
- **The roster-ghost mechanism** (distinct from aby-paluvu's heartbeat re-fire and aby-darode's stale status-file): the bridge's roster **replay** on connect hands a session *born after a peer died* that peer's stale `connect` event — without the disconnect that already happened — and the later `expire` never reaches the replay-informed session, so it carries a **permanent ghost** in `mesh_peers`. Our removal code is correct (evicts on live disconnect/offline/expired/reset — conductor-bridge.ts:418/452; proven by an older session evicting the same peer live). It's a server-side replay gap. Harmless now: routing evicts instantly, and nowabu surfaces "Agent not found" the moment you actually message it. **Roster is advisory; routing is truth.** Decided against a client-side prune (false-drop risk for live-but-quiet peers).
