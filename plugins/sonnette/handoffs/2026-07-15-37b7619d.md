# Handoff — 2026-07-15

session_id: 37b7619d-0f09-40dd-b60e-1e59a01b6e2d
purpose: A "find my old mesh glyph" question that surfaced + fixed the aby-pupaso same-cwd id collision at its root, then drift-guarded the two-language seam
format: fond-v1

## For the next Claude

### Done
- **Confirmed Bun-backed interactive mesh INBOUND** at session start: a peer (`cc-aboyeur-fd4c0f01`) sent `MESHTEST-INBOUND-92`, it surfaced as a `<channel>` tag, I ran the echo + closed the loop over the mesh. The peer also measured the clean **flag/plugin asymmetry**: transport delivery is flag-independent (reaches `events.jsonl`), but *context surfacing* is flag-gated — a flagless (MCP-via-reload) peer receives at transport but is deaf in-context. **Daemon implication:** a spawned peer that must *act* on inbound must be flag-born, not merely MCP-registered.
- **Fixed aby-pupaso at root** (closed). `deriveAgentId()` now derives the mesh id from **`CLAUDE_CODE_SESSION_ID`** (present in the MCP-spawn env — this resolved the brief's open Q1) instead of the most-recently-modified JSONL, which let same-cwd sessions adopt one uuid and supersede each other (114 flap events, both offline). JSONL scan kept as fallback for older CC. Commits: aboyeur `0c3abcb`, `~/.claude` `be399f4`.
- **Lit the `⬡` glyph for interactive sessions** — `statusline.sh` was present-but-dark (gated on `MESH_AGENT_ID`, only set for daemon peers); it now mirrors `deriveAgentId` via `CLAUDE_CODE_SESSION_ID`.
- **Verified live, not asserted:** seam match (TS `Derived:` == bash formula), live mesh registration under the new id (`peer_online`), **2-session same-cwd regression** (distinct ids, both connected, mutually visible — the exact old failure, now passing), real `statusline.sh` rendering `⬡`, and a fresh in-the-wild session (`cc-aboyeur-1c276859`) deriving its own id unprompted.
- **Drift-guarded the seam (was going to be a bon; did it instead):** extracted `meshAgentId()` to side-effect-free `src/mesh-id.ts` (one source of truth), imported by conductor-channel.ts; `src/mesh-id-seam.test.ts` pins the TS formula to literals AND runs the real `statusline.sh` against a fixture to assert both agree. Proven to go red on drift (slice 0,7), then reverted. Full suite **56/56**.
- **Updated cold-start docs:** CLAUDE.md (3 claims flipped NOT-collision-free → fixed, history kept) and understanding.md:50 (was describing the buggy JSONL-scan + the false "unique per concurrent session").
- **Filed aby-darode** (action under aby-cusera): stale-status false-glyph field report.

### Reflection
- The spine held twice: **verify the seam end-to-end, don't assert from design intent.** When Sameer flagged "I can't see the glyph and the statusline updates live," the right move was to *prove* the concrete mismatch (his server registered `a4d0ae92`, statusline computes `37b7619d`) rather than re-explain the caveat — and to symlink his true id → real mesh dir for a live look.
- I was initially **wrong** that the statusline's mesh detection was "stale/wired to the retired sidecar" — checking the source showed the new bridge deliberately preserves the same status-file contract. Glad I grepped before building on it.

### Uncertain
- **This session resumed into Vertex billing mid-close** → off the mesh (no channel tags, `mesh_peers`/`send_message` unavailable). #2 needed none of that (hermetic test), but no further *live* mesh checks were possible after the switch.

### Risks
- **Two launch paths, different deploy stories** (correction to commit `0c3abcb`'s message): `.mcp.json` → `node dist/conductor-channel.js` (needs `npm run build` after pull); `.mcp.bun.json` → `bun src/conductor-channel.ts` (no build — this session used it). The fix is in `src` and `dist` was rebuilt, so both paths carry it *on this machine*; other machines need the rebuild only for the node path.
- The statusline↔conductor-channel derivation is duplicated in two languages. It's now drift-guarded (`mesh-id-seam.test.ts`), but the guard depends on `~/.claude/statusline.sh` existing (skips gracefully otherwise).

### Opportunities
- **aby-darode** — the concrete next fix: bridge writes the status file only on transitions (not on ping), so a dead/deregistered session can show a stale `⬡`. Two-file change (touch status on ping in conductor-bridge.ts, then mtime-freshness gate in statusline.sh). Low severity.
- **aby-cusoru (Bun migration, Phase 2+)** — dissolves the dist-rebuild caveat entirely (bun runs src directly). Whoever does it must keep `statusline.sh` in lockstep with `meshAgentId` — but the new drift-guard now catches that automatically.
- One-off left in place: `/tmp/conductor-bridge/cc-aboyeur-37b7619d` symlink → this session's real dir (`a4d0ae92`), so the live glyph showed. Harmless, clears on reboot.

## For Claudes to come

- **The mesh id is derived in TWO languages that must agree** — `meshAgentId()` in `src/mesh-id.ts` (TS, used by conductor-channel.ts) and `statusline.sh` in bash (`cc-${dir}-${first8 of CLAUDE_CODE_SESSION_ID}`). They are kept in lockstep by `mesh-id-seam.test.ts`. Change one, change the other; the test proves it. Diverge and the glyph silently goes dark for a live session.
- **`CLAUDE_CODE_SESSION_ID` is the authoritative per-session identity** — CC sets it in the env for MCP servers *and* child shells (statusline) at spawn; it's race-free and stable across resume. It retired the fragile "most-recently-modified JSONL" heuristic, which collided for same-cwd concurrency. Reach for it anywhere you need to key off "this session."
- **The statusline mesh glyph reads `/tmp/conductor-bridge/{id}/status`** (written by conductor-bridge.ts on state transitions). A pre-fix running server registers under an old id, so an in-flight session won't retro-light — only *fresh* sessions light natively.
