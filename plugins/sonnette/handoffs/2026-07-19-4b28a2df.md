# Handoff — 2026-07-19

session_id: 4b28a2df-18f0-4d6b-882f-2a61f6e3fae3
purpose: Built /consult (phone-a-friend, one-shot) end-to-end, and probed the sonnette dialog-free path to a decisive verdict.
format: fond-v1

## For the next Claude

### Done
- **Proved the phone-a-friend chain live, cross-locus, solo (spike).** From one interactive session, spawned a sibling in `~/notes` (a different locus), born on the mesh, and got its consult back at the caller's bridge transport. Proves WAKE + one-shot CONSULT need NO sonnette and work from any cwd today (`--mcp-config` with the ABSOLUTE path to `conductor-channel.ts`; mesh room is account-keyed, cwd-independent). Recorded in understanding.md §Phone-a-friend.
- **Built `/consult` — a user skill in `~/.claude/skills/consult/`** (SKILL.md + `consult-spawn.sh`), lint 99/100. Dogfooded it by using it to peer-review itself; the peer found + verified 4 real bugs (FRIEND_ID collision, caller-id ignoring `$MESH_AGENT_ID`, no liveness pre-flight, mcp-config race) — all fixed and re-verified. Committed in the `~/.claude` repo, not aboyeur.
- **Ran the sonnette dialog-free probe to a decisive verdict.** Installed sonnette from a local Directory marketplace (clean, dialog-free notice, bridge connects — plugin mechanism de-risked). With channels enabled server-side, a local `/etc/claude-code/managed-settings.json` allowlist had ZERO effect (still "not on the approved allowlist"). Admin console has NO allowlist field. Cause unpinned: server-shadow OR bug #43064 (allowlist ignored for local-install plugins). Full write-up + a self-correction (my earlier "go server-side in the console" was wrong) in understanding.md §Sonnette allowlist probe. Probe torn down to clean baseline (plugin + marketplace uninstalled, /etc file removed).
- **Re-grounded `aby-lesefu` + `aby-zufefu`** to the corrected reality; filed `aby-luviwu` (graduate /consult to trousse) and `aby-pafada` (dev-flag `-m` wrapper). Removed dead `cc-tafel`/`cc-tafelv` aliases from `~/.dotfiles/shell/bashrc` (pointed at retired `tafelmusik@batterie-de-savoir`).

### Reflection
- The spine was **test the opaque layer, don't theorise it** — the probe's whole value was finding the dialog-free *wall* (allowlist gate + vendoring + an unresolved Anthropic-side issue) BEFORE we built sonnette blind into it. A probe that returns "no cheap path, here's exactly why" is a successful probe.
- I made one over-confident call mid-probe ("shadowed → set it in the admin console") and had to correct it when Sameer checked the console (no field) and the guide surfaced bug #43064. Sameer's console check was the correction channel. The corrected model is in understanding.md.
- The docs (via a claude-code-guide subagent) were **self-inconsistent** across two passes (server-shadow-Teams-needs-admin vs "/etc file-based works") — treat CC's channel/allowlist docs as unreliable here; the measurement is the truth.

### Risks
- **`aby-lesefu` and `aby-zufefu` briefs now carry the corrected model, but a future Claude skimming might still try the `/etc` local-file path** — it is proven dead on this Teams-with-switch setup. Read understanding.md §Sonnette allowlist probe before touching either.
- **`/consult`'s Step-1 precondition (`status=connected`) is insufficient** — it passes for a bridge-connected-but-NOT-flag-born caller (like a plain Vertex session such as this one), yet the peer reply never surfaces as a `<channel>` tag (only at transport). Captured in `aby-luviwu`. Until fixed, `/consult`'s reply-as-peer-tag only works from a flag-born (mesh-at-birth) session.

### Opportunities
- **`aby-pafada`** — the dev-flag `-m` wrapper: the ONE cheap thing that makes mesh-at-birth (and therefore `/consult`'s peer reply) work today, with a per-launch dialog. Highest leverage next.
- **`aby-zufefu`** — the real dialog-free build: vendor sonnette's deps, publish to `batterie`, then `aby-lesefu` can finally retest the allowlist (and distinguish #43064 from server-shadow).
- **`aby-luviwu`** — graduate `/consult` to trousse (distribution + the flag-born-caller fix).
- **`aby-rinisa`** is now largely *answered* by this session's spike + probe (the see/consult/wake model is validated by running it; the sonnette verdict is recorded) — it could be closed with a pointer to understanding.md rather than run as a fresh design session.

## For Claudes to come

The durable knowledge is already in understanding.md (§Phone-a-friend proven end-to-end, §Sonnette allowlist probe). The one framing worth carrying: **the mesh-capability ladder has three rungs, and they cost very differently.** (1) One-shot `/consult` — wake a sibling, get a peer answer — is FREE and works today with no sonnette, from any cwd. (2) Mesh-at-birth (an interactive session that RECEIVES inbound as `<channel>` tags) needs the dev flag today (one dialog) or sonnette later; a bridge-connected session is NOT automatically inbound-capable — only a flag-born/allowlisted one is. (3) Dialog-free-everywhere (sonnette) is a real vendoring + server-side-allowlist build gated by an unresolved Anthropic issue (#43064 / #58152), NOT a quick allowlist entry. Don't conflate the rungs — most of what feels blocked is only rung 3; rung 1 already delivers the original "phone a friend for a review" ask.
