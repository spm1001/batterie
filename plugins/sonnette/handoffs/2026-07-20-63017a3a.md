# Handoff — 2026-07-20

session_id: 63017a3a-9994-4181-bb69-416d20c9174a
purpose: Shipped the mesh distribution layer — sonnette plugin (aby-zufefu), claudem/-m mesh-at-birth (aby-pafada), the same-id flap fix (aby-suwawo) — and resolved the allowlist question to a measured external block (aby-lesefu).
format: fond-v1

## For the next Claude

### Done
- **aby-zufefu — sonnette shipped** as `plugin:sonnette@batterie` (suite 1.16.0). Vendoring solved with a committed single-file bundle (`sonnette/conductor-channel.js`, `npm run build:sonnette`) + a bun-locating wrapper (`sonnette/run.sh`) that fails LOUD when bun is absent. Wired into batterie `assemble.sh` (PLUGINS `sonnette:aboyeur` + `/dist` exclude), marketplace.json, and batterie-lint REPO_NAMES. CI `sonnette-bundle-fresh` diffs the bundle against a pinned-bun rebuild.
- **aby-pafada — mesh-at-birth wrappers** (`~/.dotfiles/shell/bashrc`): `claudem` (plain Teams) + `-m` on `claudev`/`claudeov`/`claudefv` append `--dangerously-load-development-channels plugin:sonnette@batterie`. Verified live: a `claudem` session received channel tags and its ACK landed at the caller's transport, cross-verified. Vertex can't bind channels (measured — provider policy); `-m` left inert-but-self-documenting there.
- **aby-lesefu — allowlist verdict: SERVER-SHADOW confirmed, #43064 eliminated.** Retested with the real marketplace install: both schemas denied, `/status` shows only "Enterprise managed settings (remote)" as a source — the `/etc` file is never read. Parked as Waiting For (Anthropic #58152 / a Teams console allowlist field). Docs cross-check (`code.claude.com/docs/en/channels{,-reference}.md`) confirmed the schema was right.
- **aby-suwawo — same-id double-server war FIXED** (suite 1.16.1). Root cause verified from source: `recoveryAttempted` re-arms on every successful `conductor_connected`, so aby-tarafo's yield + health-check-revive combined into a perpetual ~10s war between two same-id servers (project `.mcp.json` + plugin, both in aboyeur's cwd). Fix: shared `owner.pid` (`pid:birthMs`); a process yields permanently iff a live, strictly *older* sibling owns the id. Age (not liveness) is the discriminator. Two-process real-mesh test committed: `tests/suwawo-two-process.mjs`.
- **aby-funupo filed** — daemon tests red on tube (better-sqlite3 native ABI mismatch, `npm rebuild better-sqlite3` fixes; low urgency, daemon is Someday/Maybe).
- **aby-kisemi filed** — Sameer's statusline hollow/filled capability indicator idea.
- CLAUDE.md + understanding.md swept to current reality.

### Reflection
- The spine tonight was **verify from the substrate, at the real layer.** aby-suwawo's mechanism was re-derived from the code (not the handoff's guess), and the fix was proven with a two-process harness that FIRST caught the war on old code, THEN caught a real regression in my own first fix (a liveness-only rule raced and broke the tarafo revive) before it shipped. Demand-the-failure earned its keep.
- Sameer's live play-testing drove three findings: the double-server flap (he asked "I didn't realise flapping was still a thing?" → it was newly self-inflicted, not a tarafo regression), the Vertex channel boundary, and the statusline idea.
- Honest gap: `npm test` is NOT fully green — the daemon half is 29 red on the pre-existing better-sqlite3 ABI mismatch (aby-funupo). The channel suite (my area) is 8/8.

### Risks
- **A future Claude working IN aboyeur with sonnette installed** runs two conductor servers (project `.mcp.json` + plugin), same id — no longer flaps (1.16.1) but one yields. If mesh acts oddly *in this repo specifically*, that's why: `claude plugin disable sonnette` locally or work from another cwd. This is aboyeur-cwd-ONLY; real users and other cwds run one server.
- **owner.pid pid-reuse caveat** (documented in code): a dead owner pid reused in the sub-second race window could mislead the yield decision; failure mode is "this session doesn't mesh" — recoverable.
- **Any src/ mesh edit needs `npm run build:sonnette` + commit the bundle**, or CI goes red (and a stale bundle would ship). The dev hot-path (`.mcp.json` → `bun src/`) is unaffected; only the shipped plugin uses the bundle.

### Opportunities
- **aby-kisemi** — the statusline capability indicator (hollow/filled). Tier-1 is a ~15-line statusline.sh edit (flag-in-cmdline + existing billing check). Highest-leverage small win; pairs naturally with aby-darode (liveness axis).
- **aby-lezuhu** — the Bun-migration docs sweep (README, MESH-SETUP, AGENTS.md, conductor.sh); last open phase of aby-cusoru.
- **aby-luviwu** — graduate `/consult` to trousse (+ the flag-born-caller fix).
- **aby-rinisa** — largely answered by the shipped ladder; could close with a pointer to understanding.md.
- **aby-lesefu** re-test only after a CC changelog / Teams console change touches channels or managed settings — dead until then.

## For Claudes to come

The durable knowledge is in `.bon/understanding.md` (§mesh-capability ladder, §Sonnette SHIPPED, §Allowlist verdict, the double-server landmine bullet) and now CLAUDE.md's Mesh section. The one framing to carry: **the three-rung ladder is now built where it can be and blocked where it can't.** Rung 1 (one-shot `/consult`) and rung 2 (mesh-at-birth via `claudem`/`-m`, one dialog) are SHIPPED and work today; rung 3 (dialog-free everywhere) is not a build problem any more — sonnette is a real, properly-packaged plugin — it's an Anthropic-side gate (server-shadowed managed-settings on Teams; #58152). Don't spend effort trying to make the `/etc` allowlist work on this setup; it's measured dead. And a repo-specific trap that will outlive this session: **aboyeur is the one cwd that runs its own conductor server via `.mcp.json`, so it's the one place the plugin double-loads** — every mesh oddity seen *in aboyeur* should suspect that first.
